# WebEmulator
This repo contains the binjgb runtime assets and several static HTML launchers for emulating the fixed `gameboy.gb` ROM.

Open `control-room.html` for the balanced launcher, `minimal.html` for the fast path, `debug.html` for the tuning-heavy view, or `lab.html` for the full reference surface.
